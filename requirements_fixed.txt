# 修复版本的依赖文件
# 解决numpy版本兼容性问题

# 核心深度学习框架
torch>=1.8.0
torchvision>=0.9.0  # 添加torchvision依赖

# 科学计算库 - 使用兼容的版本
numpy>=1.21.0,<1.25.0  # 放宽numpy版本限制，避免兼容性问题
pandas>=1.3.0,<2.0.0   # 使用与numpy兼容的pandas版本
scipy>=1.7.0,<1.10.0   # 兼容的scipy版本

# 生物信息学库
scanpy>=1.8.0,<2.0.0
anndata>=0.8.0,<0.9.0

# 机器学习库
scikit-learn>=1.0.0,<1.3.0
scikit-misc>=0.2.0

# 其他工具
tqdm>=4.60.0
matplotlib>=3.3.0,<3.6.0

# 文档生成
nbsphinx>=0.8.0
sphinx_rtd_theme>=1.0.0
sphinx_autodoc_typehints>=1.12.0
